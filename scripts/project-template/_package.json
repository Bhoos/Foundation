{
  "name": "{{name}}",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "foundation start"
  },
  "peerDependencies": {
    "react": "{{reactVersion}}",
    "react-native": "{{reactNativeVersion}}"
  },
  "devDependencies": {
  },
  "jest": {
    "preset": "react-native"
  }
}